class CartItem {
    constructor(quantity, productPrice, productTitle, productImg, sum)
    {
        this.quantity = quantity;
        this.productPrice = productPrice;
        this.productTitle = productTitle;
        this.productImg = productImg;
        this.sum = sum;
    }
}

export default CartItem;